{
  "name": "convert-to-color",
  "private": true,
  "packageManager": "pnpm@10.30.3",
  "engines": { "node": ">=22" },
  "pnpm": { "onlyBuiltDependencies": ["esbuild"] },
  "repository": { "type": "git", "url": "git+https://github.com/freeappstore-online/convert-to-color.git" },
  "scripts": {
    "dev": "pnpm --filter @convert-to-color/web dev",
    "build": "pnpm --filter @convert-to-color/web build",
    "preview": "pnpm --filter @convert-to-color/web preview",
    "typecheck": "pnpm --filter @convert-to-color/web exec tsc -b",
    "test": "pnpm --filter @convert-to-color/web exec tsc -b"
  }
}
